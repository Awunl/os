# 🐚 MyShell — Простая оболочка на языке C

Простая, но функциональная командная оболочка (shell), написанная на языке C, поддерживающая основные возможности современных shell-интерпретаторов.

## 📁 Структура проекта

### Основные модули

| Файл | Назначение |
|------|------------|
| `main.c` | Главный цикл оболочки, обработка ввода/вывода |
| `list.c` / `list.h` | Работа со списками слов (токенов) |
| `tree.c` / `tree.h` | Построение и обработка синтаксического дерева команд |
| `exec.c` / `exec.h` | Выполнение команд и управление процессами |

### Примеры команд

| Файл | Команда | Описание |
|------|---------|----------|
| `cmp.c` | `cmp` | Сравнение двух файлов |
| `echo.c` | `echo` | Вывод текста с опциями |
| `ls.c` | `ls` | Просмотр содержимого директории |
| `pwd.c` | `pwd` | Текущая рабочая директория |
| `cat.c` | `cat` | Вывод содержимого файлов |
| `mv.c` | `mv` | Перемещение/переименование файлов |

## 🚀 Быстрый старт

### Компиляция

```bash
# Компиляция оболочки
gcc -o myshell main.c list.c tree.c exec.c

# Компиляция дополнительных утилит (опционально)
gcc -o cmp cmp.c
gcc -o echo echo.c
gcc -o ls ls.c
gcc -o pwd pwd.c
gcc -o cat cat.c
gcc -o mv mv.c

# Копирование утилит в путь (для запуска из оболочки)
cp cmp echo ls pwd cat mv /usr/local/bin/  # требует sudo
```

### Запуск

```bash
# Запуск оболочки
./myshell

# Для выхода из оболочки
exit
```

## 🛠️ Функциональность оболочки

### Поддерживаемые возможности

1. **Базовый синтаксис команд:**
   - Простые команды: `ls -la`
   - Последовательности команд: `cmd1; cmd2`
   - Условное выполнение: `cmd1 && cmd2`, `cmd1 || cmd2`

2. **Перенаправление ввода/вывода:**
   - `>` — перенаправление вывода
   - `>>` — добавление в конец файла
   - `<` — перенаправление ввода

3. **Конвейеры (pipes):**
   - `cmd1 | cmd2 | cmd3` — цепочка команд

4. **Фоновый режим:**
   - `&` — выполнение команды в фоне

5. **Группировка команд:**
   - `(cmd1; cmd2)` — выполнение в подпроцессе

6. **Переменные окружения:**
   - Подстановка `$VAR`: `echo $HOME`

### Встроенные команды

| Команда | Описание | Пример |
|---------|----------|--------|
| `cd` | Смена директории | `cd /home/user` |
| `exit` | Выход из оболочки | `exit` |

## 📖 Использование

### Примеры команд

```bash
# Простая команда
myshell> ls -la

# Последовательность команд
myshell> cd /tmp; ls; pwd

# Конвейер
myshell> cat file.txt | grep "text" | wc -l

# Перенаправление
myshell> ls > output.txt
myshell> sort < input.txt > sorted.txt
myshell> echo "text" >> log.txt

# Условное выполнение
myshell> mkdir test && cd test
myshell> rm file || echo "File not found"

# Фоновый режим
myshell> sleep 10 &

# Подстановка переменных
myshell> echo "Home directory: $HOME"
myshell> echo "Current user: $USER"

# Группировка в подпроцессе
myshell> (cd /tmp; ls) && pwd
```

## 🏗️ Архитектура проекта

### Модуль `list` (list.c/h)
- **Назначение:** Лексический анализ входной строки
- **Функции:**
  - Разбиение строки на токены (слова)
  - Обработка кавычек (одинарных и двойных)
  - Подстановка переменных окружения
  - Формирование связанного списка токенов

### Модуль `tree` (tree.c/h)
- **Назначение:** Синтаксический анализ и построение AST
- **Структуры:**
  ```c
  struct cmd_inf {
      char **argv;           // Аргументы команды
      char *infile;          // Файл для ввода
      char *outfile;         // Файл для вывода
      int append;            // Флаг дополнения файла
      int backgrnd;          // Флаг фонового режима
      struct cmd_inf *psubcmd; // Подкоманда (в скобках)
      struct cmd_inf *pipe;    // Следующая команда в конвейере
      struct cmd_inf *next;    // Следующая команда в последовательности
      enum type_of_next type;  // Тип связи (NXT, AND, OR)
  };
  ```

### Модуль `exec` (exec.c/h)
- **Назначение:** Выполнение команд
- **Возможности:**
  - Запуск внешних программ (`execvp`)
  - Управление процессами (`fork`, `waitpid`)
  - Реализация конвейеров (`pipe`)
  - Обработка перенаправлений (`dup2`)
  - Фоновое выполнение и управление фоновыми процессами

### Главный модуль `main.c`
- **Назначение:** Основной цикл оболочки
- **Функции:**
  - Чтение команд с клавиатуры
  - Обработка сигнала Ctrl+C (SIGINT)
  - Координация работы модулей

## ⚙️ Системные требования

- **ОС:** Linux или другая Unix-подобная система
- **Компилятор:** GCC или Clang (стандарт C99)
- **Библиотеки:** Стандартная библиотека C, POSIX API

## 🧪 Тестирование

### Базовые тесты

```bash
# Тестирование простых команд
echo "Hello, world!"
ls -la
pwd

# Тестирование конвейеров
echo -e "apple\nbanana\ncherry" | sort
cat /etc/passwd | grep root | wc -l

# Тестирование перенаправлений
ls > filelist.txt
cat < filelist.txt
echo "new line" >> filelist.txt

# Тестирование фонового режима
sleep 5 &
echo "Background job started"
```

### Тестирование ошибок

```bash
# Несуществующая команда
nonexistent_command

# Неправильный синтаксис
ls | |
echo "test
```

## 🔧 Сборка с отладочной информацией

```bash
gcc -g -Wall -Wextra -std=c99 -o myshell_debug main.c list.c tree.c exec.c
gdb ./myshell_debug
```

## 📊 Ограничения реализации

### Текущие ограничения

1. **Синтаксис:**
   - Нет поддержки подстановки команд (backticks или `$()`)
   - Ограниченная обработка escape-последовательностей
   - Нет поддержки алиасов и функций оболочки

2. **Управление процессами:**
   - Нет управления заданиями (jobs, fg, bg)
   - Ограниченная обработка сигналов
   - Нет поддержки завершения по Tab

3. **Переменные:**
   - Только переменные окружения, без локальных переменных
   - Нет арифметических вычислений
   - Нет расширений параметров

4. **Безопасность:**
   - Нет контроля доступа
   - Нет защиты от инъекций
   - Ограниченная обработка ошибок

## 🚧 Известные проблемы

1. Утечки памяти при некоторых ошибках синтаксиса
2. Не всегда корректная обработка вложенных кавычек
3. Ограниченная обработка специальных символов в именах файлов
4. Нет поддержки многострочных команд

## 📈 Возможные улучшения

### Планируемые функции
- [ ] Поддержка истории команд
- [ ] Завершение по Tab
- [ ] Управление заданиями (jobs, fg, bg)
- [ ] Локальные переменные и арифметика
- [ ] Подстановка команд (`$(cmd)`)
- [ ] Поддержка циклов и условий
- [ ] Сценарии оболочки (скрипты)

### Оптимизации
- [ ] Кэширование путей для быстрого поиска команд
- [ ] Оптимизация управления памятью
- [ ] Асинхронное выполнение длительных операций
- [ ] Поддержка цветного вывода

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/amazing-feature`)
3. Зафиксируйте изменения (`git commit -m 'Add amazing feature'`)
4. Отправьте в оригинальный репозиторий (`git push origin feature/amazing-feature`)
5. Создайте Pull Request

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. См. файл `LICENSE` для получения дополнительной информации.

## 📚 Ресурсы для изучения

### Книги
- "The Linux Programming Interface" by Michael Kerrisk
- "Advanced Programming in the UNIX Environment" by W. Richard Stevens
- "The Design of the UNIX Operating System" by Maurice J. Bach

### Стандарты
- POSIX.1-2017 стандарт
- ISO/IEC 9945 (Single UNIX Specification)

---
