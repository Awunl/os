# 🐚 Unix-утилиты на C

Набор консольных утилит, реализованных на языке C, имитирующих поведение стандартных команд Unix/Linux. Проект создан для изучения системного программирования и работы с файловой системой.

## 📦 Содержимое проекта

| Утилита | Файл | Описание |
|---------|------|----------|
| **cmp** | `cmp.c` | Сравнение двух файлов |
| **echo** | `echo.c` | Вывод текста с опциями |
| **ls** | `ls.c` | Просмотр содержимого директории |
| **pwd** | `pwd.c` | Текущая рабочая директория |
| **cat** | `cat.c` | Вывод содержимого файлов |
| **mv** | `mv.c` | Перемещение/переименование файлов |

## 🚀 Быстрый старт

### Компиляция

```bash
# Компиляция всех утилит
gcc -o cmp cmp.c
gcc -o echo echo.c
gcc -o ls ls.c
gcc -o pwd pwd.c
gcc -o cat cat.c
gcc -o mv mv.c

# Или используйте скрипт сборки
chmod +x build.sh
./build.sh
```

### Установка

```bash
# Копирование в /usr/local/bin (требует sudo)
sudo cp cmp echo ls pwd cat mv /usr/local/bin/

# Или установка в домашнюю директорию
mkdir -p ~/bin
cp cmp echo ls pwd cat mv ~/bin/
export PATH=$PATH:~/bin
```

## 📖 Документация утилит

### 🔍 cmp — сравнение файлов

**Сравнивает два файла побайтово и показывает первое различие.**

```bash
./cmp файл1 файл2
```

**Примеры:**
```bash
./cmp data.txt backup.txt
# Вывод: Файлы идентичны
# Или: data.txt отличается от backup.txt: строка 5, символ 12
```

**Возвращаемые коды:**
- `0` — файлы идентичны
- `1` — файлы различны
- `-1` — ошибка открытия файла

### 📢 echo — вывод текста

**Выводит текст с поддержкой escape-последовательностей.**

```bash
./echo [опции] [текст...]
```

**Опции:**
- `-n` — не добавлять перевод строки в конце
- `-e` — интерпретировать escape-последовательности

**Escape-последовательности:**
- `\n` — перевод строки
- `\t` — табуляция
- `\\` — обратный слеш

**Примеры:**
```bash
./echo "Hello World"
# Hello World

./echo -n "No newline"
# No newline%

./echo -e "Line 1\nLine 2"
# Line 1
# Line 2
```

### 📁 ls — список файлов

**Отображает содержимое директории с различными форматами вывода.**

```bash
./ls [опции]
```

**Опции:**
- `-l` — подробный вывод (длинный формат)
- `-g` — показывать только группу (без владельца)
- `-R` — рекурсивный обход поддиректорий

**Примеры:**
```bash
./ls                # Простой список
./ls -l             # Подробный вывод
./ls -l -R          # Рекурсивный обход с деталями
./ls -g             # Только информация о группе
```

**Формат вывода `-l`:**
```
drwxr-xr-x  8 user group  4096 Dec 20 10:30 dirname
-rw-r--r--  1 user group  1024 Dec 19 15:45 filename.txt
lrwxrwxrwx  1 user group    12 Dec 18 09:20 link -> target
```

### 📍 pwd — текущая директория

**Выводит абсолютный путь текущей рабочей директории.**

```bash
./pwd
```

**Пример:**
```bash
./pwd
# /home/user/projects/unix-utils
```

### 🐈 cat — конкатенация файлов

**Выводит содержимое одного или нескольких файлов.**

```bash
./cat [опции] [файлы...]
```

**Опции:**
- `-n` — нумеровать все строки вывода

**Примеры:**
```bash
./cat file.txt              # Вывести содержимое файла
./cat -n file.txt           # Вывести с нумерацией строк
./cat file1.txt file2.txt   # Объединить несколько файлов
./cat -n < file.txt         # Чтение из stdin
```

**Формат вывода с `-n`:**
```
     1  Первая строка
     2  Вторая строка
     3  Третья строка
```

### 🚚 mv — перемещение файлов

**Перемещает или переименовывает файлы и директории.**

```bash
./mv источник назначение
```

**Примеры:**
```bash
./mv oldname.txt newname.txt      # Переименование
./mv file.txt /home/user/docs/    # Перемещение в директорию
./mv *.txt backup/                # Перемещение нескольких файлов
```

## 🛠 Технические детали

### Требования к системе

- **ОС:** Linux, macOS, или другая Unix-подобная система
- **Компилятор:** GCC или Clang (стандарт C99 или новее)
- **Библиотеки:** Стандартная библиотека C, POSIX

### Ограничения реализации

1. **ls:**
   - Поддержка только основных флагов (`-l`, `-g`, `-R`)
   - Отсутствие сортировки по алфавиту (только по дате в `-l`)
   - Ограниченная обработка специальных символов в именах файлов

2. **echo:**
   - Поддержка только базовых escape-последовательностей
   - Нет обработки восьмеричных/шестнадцатеричных escape-кодов

3. **Общие:**
   - Ограниченная обработка ошибок ввода/вывода
   - Базовые проверки аргументов командной строки
   - Минимальная поддержка локалей и кодировок

### Структура кода

Каждая утилита является самостоятельным исполняемым файлом с:

1. **Обработкой аргументов** командной строки
2. **Проверкой ошибок** ввода/вывода
3. **Информативными сообщениями** об ошибках (на русском языке)
4. **Корректным освобождением** ресурсов

## 🧪 Тестирование

### Примеры тестов

```bash
# Тестирование cmp
echo "test1" > file1.txt
echo "test2" > file2.txt
./cmp file1.txt file2.txt

# Тестирование ls
mkdir test_dir
touch test_dir/{a,b,c}.txt
./ls -l test_dir/

# Тестирование cat
echo -e "Line 1\nLine 2\nLine 3" > multi.txt
./cat -n multi.txt
```

## 📚 Для разработчиков

### Сборка с отладочной информацией

```bash
gcc -g -Wall -Wextra -o program program.c
```

### Создание Makefile

```makefile
CC = gcc
CFLAGS = -Wall -Wextra -std=c99
TARGETS = cmp echo ls pwd cat mv

all: $(TARGETS)

%: %.c
	$(CC) $(CFLAGS) -o $@ $<

clean:
	rm -f $(TARGETS)

install: all
	sudo cp $(TARGETS) /usr/local/bin/
```

## 🐛 Известные проблемы

1. **ls:** При рекурсивном обходе может не обрабатывать циклы символических ссылок
2. **mv:** Не поддерживает опции для принудительного перезаписывания
3. **cat:** Не обрабатывает бинарные файлы корректно
4. **echo:** Флаг `-e` должен быть первым аргументом для правильной работы

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/amazing-feature`)
3. Зафиксируйте изменения (`git commit -m 'Add amazing feature'`)
4. Отправьте в оригинальный репозиторий (`git push origin feature/amazing-feature`)
5. Создайте Pull Request

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. См. файл `LICENSE` для получения дополнительной информации.

## 📝 Примечания

- Проект создан в образовательных целях
- Не предназначен для использования в производственных средах без дополнительной доработки
- Русскоязычные сообщения об ошибках могут быть изменены на английские для портативности

---
*Проект демонстрирует основы системного программирования на C и взаимодействие с файловой системой Unix.*
